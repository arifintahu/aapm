{
  "name": "aapm-workspace",
  "version": "1.0.0",
  "description": "Gasless Prediction Market Betting Platform - Multi-project workspace",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm install --prefix contracts && npm install --prefix backend && npm install --prefix frontend",
    "dev:contracts": "npm run node --prefix contracts",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "build:contracts": "npm run compile --prefix contracts",
    "build:backend": "npm run build --prefix backend",
    "build:frontend": "npm run build --prefix frontend",
    "build:all": "npm run build:contracts && npm run build:backend && npm run build:frontend",
    "test:contracts": "npm run test --prefix contracts",
    "lint:frontend": "npm run lint --prefix frontend",
    "clean": "rimraf contracts/artifacts contracts/cache contracts/typechain-types backend/dist frontend/dist"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "rimraf": "^6.0.1"
  },
  "keywords": ["blockchain", "ethereum", "prediction-market", "web3", "gasless", "workspace"],
  "author": "",
  "license": "MIT"
}
